<UserControl x:Class="ContactsApps.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ContactsApps.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Margin="5">
            <TextBlock Name="nameTextBlock" Text="{Binding contactName}"  FontSize="20" FontWeight="Bold"/>
            <TextBlock Name="emailTextBlock" Text="{Binding contactEmail}" FontSize="15" Foreground="DodgerBlue"/>
            <TextBlock Name="phoneTextBlock" Text="{Binding contactPhone}" FontSize="15" FontStyle="Italic"/>
        </StackPanel>
    </Grid>
</UserControl>
